# Payments Only

Common query parameter used by 4 endpoints.

```omg.query
{
  paymentsOnly?: boolean  // Filter to retrieve journals on a cash basis. Journals are returned on an accrual basis by default.
}
```
