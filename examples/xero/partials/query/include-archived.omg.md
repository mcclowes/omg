# Include Archived

Common query parameter used by 3 endpoints.

```omg.query
{
  includeArchived?: boolean  // e.g. includeArchived=true - Contacts with a status of ARCHIVED will be included in the response
}
```
